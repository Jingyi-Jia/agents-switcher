[project]
name = "agents-switcher"
version = "1.2.0"
description = "Multi-account switcher for Claude Code and Codex"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # Windows-only: needed solely by the one-time keyring -> files migration
    # (migrations.py) for users upgrading from <=0.10.x. macOS/Linux never
    # import it on the hot path; the macOS migration has a security-CLI
    # fallback and the legacy purge sweep no-ops when it's absent.
    "keyring>=25.0.0; sys_platform == 'win32'",
    "textual>=8.2.8,<9",
    "truststore>=0.10.4",
]
# Both are true and both are stated: claude-swap is Onur's; the Codex side,
# the dashboard, launcher, tray and dirlock are additions maintained here.
authors = [
    {name = "Onur Cetinkol", email = "onurcetinkol@gmail.com"},
    {name = "Jingyi Jia"},
]
maintainers = [{name = "Jingyi Jia"}]
license = {text = "MIT"}
keywords = ["claude", "claude-code", "account-switcher", "cli"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: MacOS",
    "Operating System :: Microsoft :: Windows",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Utilities",
]

[project.optional-dependencies]
menubar = ["rumps>=0.4.0"]
# Tray/menu-bar readout. Split by platform because the toolkits are not
# interchangeable: rumps renders VISIBLE menu-bar text on macOS, while
# pystray's macOS backend only sets a tooltip -- and an at-a-glance number
# you have to hover for is not at a glance.
tray-macos = ["rumps>=0.4.0"]
tray = ["pystray>=0.19", "pillow>=10"]

[project.urls]
Homepage = "https://github.com/Jingyi-Jia/agents-switcher"
Repository = "https://github.com/Jingyi-Jia/agents-switcher"
Issues = "https://github.com/Jingyi-Jia/agents-switcher/issues"
# The origin. Issues about the Claude Code side may well belong there instead,
# but a bug report about a feature that only exists here must not land in the
# original author's tracker.
Upstream = "https://github.com/realiti4/claude-swap"

[project.scripts]
# Deliberately NOT `cswap`/`claude-swap`: upstream claude-swap installs those,
# and a fork that shadows them on PATH makes which tool ran depend on install
# order. A distinct distribution name keeps `uv tool install` separate too.
agent-switch = "claude_swap.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/claude_swap"]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-asyncio>=0.24", "pytest-xdist>=3.5", "trustme>=1.2.1"]
desktop-build = ["pyinstaller==6.22.3"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
# Parallel by default. The suite is ~1900 tests of mostly-idle work (file
# locks, subprocess fakes, Textual pilots), so wall clock was dominated by
# doing them one at a time, not by any single slow test: 25 ms/test spread
# across the whole suite, with the top five totalling under 7 s of a 51 s run.
# Measured here, 48 cores: 51.0s serial -> 12.1s (-n 8) -> 7.7s (-n 16), same
# 1929 passed / 4 skipped every time, three consecutive runs, real account
# store hash unmoved. `auto` rather than a fixed number so CI runners (2-4
# cores) and this box both get the cores they have.
addopts = "-n auto --dist loadgroup"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "native_process: probes real OS process metadata; run serially after the other tests finish",
    "no_keychain_fake: opt out of the autouse in-memory Keychain guard (test mocks subprocess itself, or runs against a temp keychain on GitHub Actions)",
    "no_oauth_profile_fake: opt out of the autouse fetch_oauth_profile stub (test exercises fetch_oauth_profile itself against a mocked urlopen)",
]
